#version 330 core
out vec4 FragColor;
uniform vec4 color;
uniform vec3 lightColor;
void main()
{
    float strength = 1.0;
    vec3 color_ = vec3(color.x, color.y, color.z);
    vec3 ambient = strength * lightColor * color_; 
    FragColor = vec4(ambient, 1.0);
}
