#version 330 core
out vec4 FragColor;
uniform vec4 color;
void main()
{
    float strength = 0.1;
    vec3 color_ = vec3(color.x, color.y, color.z);
    vec3 ambient = strength * color_; 
    FragColor = vec4(ambient, 1.0);
}
